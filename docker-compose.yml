version: "3"

services:
  db:
    image: mysql
    container_name: database
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: "root"
    volumes:
      - ./schemas:/var/lib/mysql